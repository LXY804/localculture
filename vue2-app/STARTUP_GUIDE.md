# 本地文化平台启动指南

## 🚀 快速启动

### 方法一：使用启动脚本（推荐）

#### Windows用户
双击 `start.bat` 文件，系统会自动启动前后端服务。

#### Linux/Mac用户
```bash
./start.sh
```

### 方法二：使用npm命令

1. 安装依赖（首次运行）：
```bash
npm install
```

2. 启动所有服务：
```bash
npm start
# 或者
npm run dev
```

3. 分别启动服务：
```bash
# 启动后端
npm run start:backend

# 启动前端（新开终端）
npm run start:frontend
```

## 🌐 访问地址

- **前端地址**: http://localhost:8080
- **后端API**: http://localhost:3001

## 🔧 服务说明

- **前端服务器**: Vue.js开发服务器，运行在8080端口
- **后端服务器**: Node.js + Express API服务器，运行在3001端口
- **数据库**: MySQL数据库（需要单独配置）

## ⚠️ 注意事项

1. **端口占用**: 确保8080和3001端口未被其他程序占用
2. **数据库**: 确保MySQL服务正在运行，并且数据库配置正确
3. **Node.js**: 确保已安装Node.js环境

## 🛠️ 故障排除

### 端口被占用
```bash
# Windows查看端口占用
netstat -ano | findstr :3001
netstat -ano | findstr :8080

# 杀死占用进程
taskkill /f /pid <进程ID>
```

### 数据库连接失败
检查 `server/.env` 文件中的数据库配置：
```
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=localculture
```

### 依赖安装失败
```bash
# 清除缓存重新安装
npm cache clean --force
rm -rf node_modules
npm install
```

## 📝 开发说明

- 前端代码位于 `src/` 目录
- 后端代码位于 `server/` 目录
- 数据库脚本位于 `server/schema.sql`
- 配置文件位于 `vue.config.js` 和 `server/.env`

## 🎯 默认账户

- **管理员**: admin / admin123
- **普通用户**: user / user123

---

如有问题，请检查控制台错误信息或联系开发团队。
